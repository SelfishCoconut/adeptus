# Base URL for the Adeptus backend API.
# In dev, Vite proxies /api -> http://localhost:8000 (see vite.config.ts),
# so a relative path keeps requests same-origin and the session cookie intact.
VITE_API_BASE_URL=/api/v1
